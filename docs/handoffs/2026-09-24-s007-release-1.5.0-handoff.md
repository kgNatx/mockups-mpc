# Handoff — 1.5.0 released: deferred minors, Setup Guide refresh, local RC, live fold (2026-09-24)

Session: s007

## TL;DR

Finished and shipped **1.5.0 (versioned mockups + compact sidebar)**. Folded in every deferred minor from
s006, added versions content to the in-app Setup Guide plus a startup refresh so existing installs get it,
ran two scoped review rounds (every finding fixed), validated a release candidate locally on a copy of
live data (caught one more layout bug), then merged, released, deployed, and ran the live fold.
**All four version trackers are at 1.5.0. No open follow-ups.**

## Shipped

- **Write path:** a failed COMMIT rolls back; a cancelled task whose COMMIT still ran keeps its file;
  `delete_design` is one transaction (it raced `add_version`); a failed write or close leaves no partial
  file; typed `versioning.NotFound` / `Conflict` drive 404/409; tag edits and content+tags updates are atomic.
- **Routes/MCP:** favorite by alias id; alias + wrong version says "Version not found"; parent or fold
  target deleted mid-upload → 404 / new design, not a 500.
- **Gallery UI:** stale version links open the latest; unknown id says "Mockup not found"; older-version
  pill reads `v{n} of v{latest}` (spec §8.3 amended); poll baseline comes from the rows the feed loaded,
  a failed load retries, a star no longer reloads the feed; **fixed a pre-existing bug where rows loaded by
  scrolling vanished on the next poll with >50 designs**; open version lists survive re-renders; feed-row
  buttons sit on the title line on every screen (they covered the version chip on desktop); favicon.
- **Setup Guide:** versions section, 8 tools, `parent=` line in its CLAUDE.md snippet.
  `seed.refresh_guide` runs at startup, best-effort: it adds the shipped guide as a new version when the
  stored latest is an unedited guide this app wrote (a trailing `<!-- mockups-mpc-guide sha256=… -->`
  stamp that must match the body) or a lone legacy v1. User versions, deleted, renamed or duplicate guides
  are left alone. Owner's call (M1): the refreshed guide pops to the top of the feed; CHANGELOG says so.
- **Sort orders** end with `, id` so pages and the poll probe agree on ties.
- Kyle's global `~/.claude/CLAUDE.md` Mockups section has the `-F parent=<id>` line (verified).
- `pyproject.toml` joined release step 1 (memory `project_release_process`).

## Release state

- `main` = `9bc16b4` (pushed). Merge `cb83e0e`. 229 tests pass on the merged tree.
- GHCR `1.5.0` + `latest` (same digest); GitHub Release `v1.5.0` (Latest); MCP registry 1.5.0.
- Live: migration ran (956 designs got v1), guide at v2, fold applied (51 groups → **876 designs**),
  old ids and pre-fold version links return 200, browser pass clean.
- Backup before deploy: `data/mockups.db.bak-pre-1.5.0` (956 rows, integrity ok). Keep until Kyle says.

## Explicitly NOT done / decided against

- Link minted after a fold, then broken by a re-split (`/view/C/v/3`): **kept as documented behaviour**
  (owner agreed). Fixing needs a stored former-number per version.
- `DELETE /api/mockups/{alias}` stays 404 (ruling R12, s006).
- `db.update_mockup` has no app callers; kept as a documented test/setup helper.
- Auto-fold is not mentioned in the global CLAUDE.md Mockups section (server instructions and the upload
  response cover it); one-sentence addition offered, not made.
- One root-owned file (the shipped guide v2, no user data) left in the session scratchpad `rc/`; the
  session could not delete it.

## Process lessons

- A local RC on a copy of live data found a bug synthetic data could not (long badges). Now in the
  release memory as a standing pre-deploy step.
- The registry JWT expired within a day: ask for `mcp-publisher login github` right before publishing.
- Reviewer loop: 1 review + 2 short re-reviews, report via SendMessage to `main` + inbox file; all delivered.

## Commit trail

Branch fixes `4ef92da` `b82e629` `01b8db7` `2a587eb` `c6b80ba` `0a17c41` `72489db` `6bdd3bb` `4b15fd1`
`c05d5ae` → merge `cb83e0e` → release `9bc16b4`.

## Next session kickoff

Starting s008. Paste-ready prompt:

> Kickoff for mockups-mpc s008. Read `docs/handoffs/2026-09-24-s007-release-1.5.0-handoff.md` and
> `docs/handoffs/INDEX.md`. State: 1.5.0 is released and live, all four trackers agree, no open
> follow-ups. Locks: only `app/db.py` holds SQL; `db.transaction()` is not re-entrant; releases follow
> memory `project_release_process` (4 steps, pyproject included, local RC on a live-data copy before
> deploy, registry login right before publish). Ask Kyle what to work on, and whether
> `data/mockups.db.bak-pre-1.5.0` can be deleted yet.
